# Deps for the real pipeline (pipeline/process_data.py). Runs on the free GitHub
# Actions runner, or any machine with a few GB of RAM — no paid service needed.
# EXACT, mutually-compatible pins: scikit-learn >=1.6 removed the
# `force_all_finite` arg that umap-learn 0.5.6 still calls, so stay on 1.5.x
# (with matched numpy / scipy / numba).
numpy==1.26.4
scipy==1.13.1
scikit-learn==1.5.2
numba==0.60.0
umap-learn==0.5.6
pandas==2.2.2
requests
