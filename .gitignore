# --- Python ---
__pycache__/
*.py[cod]
.venv/
venv/
env/
*.egg-info/
.ipynb_checkpoints/

# --- Pipeline working dirs & raw data (large; regenerate or re-fetch) ---
# (_data_real = fetch_volume.py output, real detail tier ~85 MB; _modal_out = volume dl)
pipeline/data_raw/
pipeline/_data_real/
pipeline/_modal_out/
pipeline/*.zip
pipeline/*.hdf5
pipeline/*.csv

# web/data/: full data (overview + detail) is committed and served same-origin by
# Pages. Use a CORS-enabled object store such as R2 when the detail tier needs
# off-repo hosting.

# --- OS / editor cruft ---
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp

# --- Node cache ---
node_modules/

# --- Logs / temp ---
*.log
.cache/
